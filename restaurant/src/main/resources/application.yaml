server:
  port: 8000
  servlet:
    context-path: /restaurants
spring:
  application:
    name: restaurant
  datasource:
    url: ${PG_CONNECTION_URL:jdbc:postgresql://localhost/restaurant}
    driver-class-name: org.postgresql.Driver
    username: ${PG_USER:postgres}
    password: ${PG_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl
    open-in-view: false
  kafka:
    bootstrap-servers:
      - ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      properties:
        value.serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
    consumer:
      auto-offset-reset: earliest
      group-id: restaurant-service
      properties:
        value.deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
        spring:
          deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
          json:
            trusted.packages: com.github.frederikpietzko.*
management:
  observations:
    annotations:
      enabled: true